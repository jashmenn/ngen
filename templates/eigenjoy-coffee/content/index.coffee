# This is the index of {{project}}. You should write documentation that looks
# something like this:
#
#     {{project}} = require '{{project}}'
#     app = {{project}}(config)
#     app.run(3000)
#
module.exports = require('./src/{{project}}')
